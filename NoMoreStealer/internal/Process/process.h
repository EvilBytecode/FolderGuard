#pragma once

#include <fltKernel.h>

namespace NoMoreStealer {  // ← Geändert

    namespace Process {

        BOOLEAN IsAllowed(PEPROCESS process);

    } // namespace Process
} // namespace NoMoreStealer